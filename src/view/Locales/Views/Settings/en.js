import tokens from '../../../../lang/en/common.json';
import viewTokens from '../../../../lang/en/SettingsView/index.json';

window.localeTokens = tokens;
Object.assign(window.localeTokens, viewTokens);
